# 构建产物
build/
build_output.txt

# ESP-IDF 自动管理的依赖（idf.py reconfigure 可恢复）
managed_components/

# ESP-IDF 自动生成配置（只保留 sdkconfig.defaults）
sdkconfig
sdkconfig.old

# IDE 配置（含本地路径）
.vscode/
.idea/

# Claude Code / OMC 本地状态
.claude/
.omc/

# 系统文件
.DS_Store
Thumbs.db
*.swp
*.swo
*~

# 临时构建脚本（含本地路径，只保留主 build.bat）
build_run.bat
build_temp.bat
build_temp2.bat
_build.bat
